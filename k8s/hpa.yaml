apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: simple-devops-dashboard
  labels:
    app: simple-devops-dashboard
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: simple-devops-dashboard
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
